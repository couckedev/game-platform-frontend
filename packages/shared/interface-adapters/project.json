{
  "name": "shared-interface-adapters",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/shared/interface-adapters/src",
  "projectType": "library",
  "tags": ["type:shared"],
  "implicitDependencies": [],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "npx tsc -p packages/shared/interface-adapters/tsconfig.lib.json"
      }
    }
  }
}