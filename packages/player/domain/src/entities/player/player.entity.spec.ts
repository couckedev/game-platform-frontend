export {};

/*
describe(Player.name, () => {
  describe(Player.authenticate, () => {
    it('should produce player authenticated event', () => {
      const nickname = Nickname.fromPersistence('nickname');
      const playerId = PlayerId.fromPersistence('nickname');
      const player = new Player(playerId, nickname);

      player.authenticate();

      expect(player.uncommittedEvents).toContainEqual();
    });
  });
});*/
