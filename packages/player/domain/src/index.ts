// Player Domain layer
// Pure business logic: entities, value objects, domain events, domain services
// No framework dependencies — this layer never imports from application, infrastructure, presentation or ui
